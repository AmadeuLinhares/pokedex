import { css } from '@/styles/stitches.config'

export const inputStyles = () =>
  css({
    fontFamily: `$roboto`,
    borderRadius: `$medium`,
    borderColor: `$gray200`,
    borderStyle: `solid`,
    px: `$spacing12`,
    py: `$spacing8`,
    width: `100%`,
  })
